security:
    # https://symfony.com/doc/current/security.html#registering-the-user-hashing-passwords
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'
    # https://symfony.com/doc/current/security.html#loading-the-user-the-user-provider
    providers:
        # used to reload user from session & other features (e.g. switch_user)
        app_user_provider:
            entity:
                class: App\Entity\User
                property: email
    firewalls:
        healthcheck:
            pattern: ^/health
            security: false
        ping:
            pattern: ^/ping
            security: false
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        main:
            login_link:
                check_route: login_check
                signature_properties: [ 'id' ]
            logout:
                path: app_logout

            # activate different ways to authenticate
            # https://symfony.com/doc/current/security.html#the-firewall

            # https://symfony.com/doc/current/security/impersonating_user.html
            switch_user:
                role: 'ROLE_ADMIN'

            remember_me:
                lifetime: 31536000
                always_remember_me: true
                signature_properties: ['updatedAt']

    # Easy way to control access for large sections of your site
    # Note: Only the *first* access control that matches will be used
    access_control:
        - { path: ^/admin, roles: [ROLE_ADMIN, ROLE_HOST] }
        - { path: ^/panel, roles: ROLE_USER }

    # Custom access denied handler
    #
    # ROLE_HOST (a workshop instructor) is a subrole of ROLE_ADMIN: every
    # admin is implicitly also a host, but a host does not get the
    # admin-only capabilities. Individual admin sections are gated by a
    # dedicated capability role (ROLE_SETTINGS, ROLE_MANAGE_USERS, ...)
    # rather than checking ROLE_ADMIN/ROLE_HOST directly in controllers or
    # templates, so access to a given section only needs to change here.
    role_hierarchy:
        ROLE_HOST:
            - ROLE_MANAGE_LESSONS
            - ROLE_MANAGE_BOOKINGS
            - ROLE_USER
        ROLE_ADMIN:
            - ROLE_HOST
            - ROLE_SETTINGS
            - ROLE_MANAGE_USERS
            - ROLE_MANAGE_PAYMENTS
            - ROLE_MANAGE_MESSAGES
            - ROLE_MANAGE_SCHEDULE
        ROLE_SUPER_ADMIN: [ROLE_ADMIN, ROLE_ALLOWED_TO_SWITCH]

when@test:
    security:
        password_hashers:
            # By default, password hashers are resource intensive and take time. This is
            # important to generate secure password hashes. In tests however, secure hashes
            # are not important, waste resources and increase test times. The following
            # reduces the work factor to the lowest possible values.
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4 # Lowest possible value for bcrypt
                time_cost: 3 # Lowest possible value for argon
                memory_cost: 10 # Lowest possible value for argon
