{% extends 'base.html.twig' %}

{% block body %}
    <main class="flex-grow">
        <div class="page-container">
            <div class="max-w-md mx-auto"><h1 class="section-title text-center">{{ 'form.register.mail_sent_header'|trans }}</h1>
                <div class="rounded-lg border bg-card text-card-foreground shadow-sm mb-8">
                    <div class="p-6">
                        <h1>{{ 'form.register.verify_mail'|trans }}</h1>
                    </div>
                </div>
            </div>
        </div>
    </main>
{% endblock %}