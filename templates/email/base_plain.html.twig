<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #222;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            background: #ffffff;
        }
        .container {
            padding: 0;
        }
        h1, h2, h3 {
            color: #222;
            margin-top: 0;
        }
        .button {
            display: inline-block;
            background-color: #444;
            color: #fff !important;
            text-decoration: none;
            padding: 10px 18px;
            border-radius: 4px;
            font-weight: 600;
            margin: 20px 0;
        }
        .footer {
            margin-top: 24px;
            text-align: center;
            font-size: 12px;
            color: #666;
        }
    </style>
</head>
<body>
<div class="container">
    {# Unbranded email wrapper: render provided content block #}
    {% block content %}
    {{ content|raw }}
    {% endblock %}
    {% set org = organization() %}
    {% if org.facebookUrl %}
        <div class="footer">
            <a href="{{ org.facebookUrl }}" style="color:#1877f2; text-decoration:none; font-weight:600;">Facebook</a>
            &mdash; {{ 'social.facebook_follow'|trans({}, 'emails') }}
        </div>
    {% endif %}
    <div class="footer">
        {{ 'emails.footer'|trans({}, 'emails') }}
    </div>
</div>
</body>
</html>
