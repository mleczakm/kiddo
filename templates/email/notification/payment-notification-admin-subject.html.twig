{% trans_default_domain 'emails' %}
{% set lesson = lessons|first %}
{{ 'payment.notification.admin.subject'|trans({'user': user.emailString})|raw }} {{ lesson.metadata.title }} {{ lesson.metadata.schedule|format_datetime(pattern='dd MMM HH:mm') }}{{ lessons|length > 1 ? 'payment.notification.and_others'|trans }}
