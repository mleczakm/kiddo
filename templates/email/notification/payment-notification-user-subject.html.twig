{% trans_default_domain 'emails' %}
{% set lesson = lessons|first %}
{{ 'payment.notification.user.subject'|trans({'user': user.emailString}) }} {{ lesson.metadata.title }}, {{ lesson.metadata.schedule|format_datetime(pattern="EEEE dd MMM") }} {{ 'at'|trans }} {{ lesson.metadata.schedule|format_datetime(pattern="HH:mm") }}{{ lessons|length > 1 ? 'payment.notification.and_others'|trans }}{{ 'payment.notification.user.thank_you'|trans }}

