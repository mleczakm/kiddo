{% extends 'admin/base.html.twig' %}

{% block title %}{{ 'admin.title'|trans }}{% endblock %}

{% block body %}
    {{ component('AdminDashboardComponent', { activeTab: activeTab|default('dashboard') }) }}
{% endblock %}
