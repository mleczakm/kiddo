{% extends 'admin/base.html.twig' %}

{% set sub_breadcrumbs = [
    { label: user.name }
] %}

{% block title %}{{ 'admin.title'|trans }} — {{ user.name }}{% endblock %}

{% block topbar %}
    {{ include('admin/partials/topbar.html.twig', { activeTab: 'users', back_url: path('app_admin_users') }) }}
{% endblock %}

{% block body %}
    {{ include('admin/user.html.twig') }}
{% endblock %}
